platform :ios, '8.0'
use_frameworks!

target 'PickerViewKit_Example' do
  pod 'PickerViewKit', :path => '../'

  target 'UnitTests' do
    inherit! :search_paths

    pod 'Nimble', '~> 7.3'
    pod 'Quick', '~> 1.3'
  end
end
